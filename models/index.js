const User = require('./user.model');
const UserConfiguration = require('./userConfiguration.model');
const Configuration = require('./configuration.model');

module.exports = {
    User,
    UserConfiguration,
    Configuration
};
